#include <bits/stdc++.h>
using namespace std;
const int mod = 1e9 + 7;
typedef long long ll;

class TreeNode
{
public:
    int data;
    Node *left, right;

    TreeNode(int root)
    {
        this->data = root;
        left = right = nullptr;
    }
};


int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    return 0;
}